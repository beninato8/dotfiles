Please make a plan to implement the Linear issue for the current branch.

If you are not already in plan mode, please switch to plan mode.

Retrieve the current issue with `linear issue view <issue-id> --json`. If it has a project or milestone, look up the corresponding details with `linear project view <project>` and `linear milestone view <milestone> --project <project>`.

Come up with a plan for how to best implement the issue, and make sure to ask any clarifying questions you need to before implementing.